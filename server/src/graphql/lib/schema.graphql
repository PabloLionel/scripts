type Course {
  _id: ID!
  title: String!
  teacher: String!
  description: String
  topic: String
}
type Query {
  "Devuelve todos los cursos"
  getCourses: [Course]
  "Devuelve un los cursos"
  getCourse(id: ID!): Course
}
